# yaml-language-server: $schema=https://raw.githubusercontent.com/chainguard-dev/melange/refs/heads/main/pkg/config/schema.json
# Generated from https://raw.githubusercontent.com/alpinelinux/aports/refs/heads/master/testing/kubeconform/APKBUILD
package:
  name: kubeconform
  version: 0.7.0
  epoch: 1 # go-package + tightened test
  description: A FAST Kubernetes manifests validator, with support for Custom Resources!
  copyright:
    - license: Apache-2.0
environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go-1.25
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/yannh/kubeconform
      tag: v${{package.version}}
  - uses: go/build
    with:
      go-package: go-1.25
      packages: ./cmd/kubeconform
      output: kubeconform
  - uses: strip
test:
  pipeline:
    - uses: test/tw/ldd-check
    - name: Verify kubeconform
      runs: |
        set -eu
        kubeconform -v
        kubeconform -h
        cat <<'EOF' | kubeconform -
        apiVersion: v1
        kind: ConfigMap
        metadata:
          name: test
        data:
          key: value
        EOF
