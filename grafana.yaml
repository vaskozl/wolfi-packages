package:
  name: grafana
  version: "13.0.2"
  epoch: 0
  # When bumping, check if we can unpin nodejs-22.
  # Remove this comment when nodejs-22 is unpinned.
  description: The open and composable observability and data visualization platform.
  copyright:
    - license: AGPL-3.0-or-later
  dependencies:
    runtime:
      - bash
  resources:
    cpu: "15"
    memory: 31Gi
environment:
  contents:
    packages:
      - build-base
      - busybox
      - findutils
      - go
      - nodejs-22
      - npm
      - posix-libc-utils
      - py3-setuptools
      - python3
      - yarn
      - zlib-dev
  environment:
    PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: 1
var-transforms:
  - from: ${{package.version}}
    match: ^(\d+\.\d+\.\d+)\.(\d+)$
    replace: "${1}+security-${2}"
    to: mangled-package-version
  - from: ${{package.version}}
    match: ^(\d+\.\d+)\.\d+(\.\d+)?$
    replace: "$1"
    to: major-minor-version
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/grafana/grafana
      tag: v${{vars.mangled-package-version}}
      expected-commit: 3fcdbc5a6e5c955bd42bd3715dd03cbad2b078c1
  - runs: |
      # Upstream's Makefile generates BUILD_VERSION from the version in
      # package.json, but upstream are inconsistent about committing the
      # appropriate content.  BUILD_VERSION ends up in CLI version output, and
      # we've had customer escalations due to this mismatch.
      sed -i -E 's|^(\s*"version":\s*)"[^"]+"|\1"${{vars.mangled-package-version}}"|' package.json
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: |-
        apps/alerting/historian
        apps/annotation
        apps/live
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        github.com/go-jose/go-jose/v4@v4.1.4
        github.com/jackc/pgx/v5@v5.9.2
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: |-
        apps/dashvalidator
        apps/provisioning
        apps/quotas
        pkg/apimachinery
        pkg/plugins
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: pkg/apiserver
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: pkg/aggregator
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/shorturl
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/plugins
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/playlist
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/logsdrilldown
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/example
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/dashboard
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/correlations
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/alerting/rules
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/alerting/notifications
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        github.com/aws/aws-sdk-go-v2/aws/protocol/eventstream@v1.7.8
        github.com/aws/aws-sdk-go-v2/service/cloudwatchlogs@v1.65.0
        github.com/aws/aws-sdk-go-v2/service/s3@v1.97.3
        github.com/Azure/go-ntlmssp@v0.1.1
        github.com/jackc/pgx/v5@v5.9.2
        github.com/openfga/openfga@v1.14.1
        go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc@v0.68.0
        go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp@v0.68.0
        go.opentelemetry.io/otel/exporters/otlp/otlplog/otlploghttp@v0.19.0
        go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetrichttp@v1.43.0
        go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracehttp@v1.43.0
        go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc@v1.43.0
        go.opentelemetry.io/otel/sdk@v1.43.0
        google.golang.org/grpc@v1.80.0
        github.com/openfga/api/proto@v0.0.0-20260319214821-f153694bfc20
        github.com/pressly/goose/v3@v3.27.0
        go.opentelemetry.io/otel@v1.43.0
        go.opentelemetry.io/otel/trace@v1.43.0
        modernc.org/sqlite@v1.48.2
        github.com/openfga/language/pkg/go@v0.2.1
        github.com/apache/thrift@v0.23.0
        golang.org/x/crypto@v0.52.0
      work: true
  - uses: go/bump
    with:
      deps: |-
        github.com/go-jose/go-jose/v4@v4.1.4
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/iam
      tidy: false
  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.43.0
      modroot: apps/advisor
      tidy: false
  - runs: |
      # Update the workspace to the Go version required by dependencies: `make
      # build` fails without this, and we can't use `go work use` because it's
      # a noop until some parts of `make build` have run
      # Get the current go version from the go.mod file and then update go.work
      GO_VERSION=$(grep '^go ' go.mod | awk '{print $2}')
      sed -i "s/go [0-9]\+\.[0-9]\+\.[0-9]\+/go ${GO_VERSION}/" go.work
  - name: Build
    runs: |
      export NODE_OPTIONS="--max-old-space-size=8192"
      yarn install

      # Builds frontend and backend (build-go build-js)
      make build
  - name: package
    runs: |
      mkdir -p ${{targets.destdir}}/usr/bin
      mkdir -p ${{targets.destdir}}/usr/share/grafana
      mkdir -p ${{targets.destdir}}/etc/grafana

      cp bin/linux/*/* ${{targets.destdir}}/usr/bin/
      cp -r conf public ${{targets.destdir}}/usr/share/grafana/

      mkdir -p ${{targets.destdir}}/usr/share/grafana/scripts
      mkdir -p ${{targets.destdir}}/usr/share/grafana/bin

      cp conf/sample.ini ${{targets.destdir}}/etc/grafana/grafana.ini
      cp conf/sample.ini ${{targets.destdir}}/etc/grafana/grafana.ini.example
subpackages:
  - name: ${{package.name}}-oci-compat
    dependencies:
      provides:
        - grafana-oci-compat=${{package.full-version}}
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/opt/grafana
          mv ./packaging/docker/run.sh ${{targets.subpkgdir}}/opt/grafana/
    test:
      pipeline:
        - uses: test/virtualpackage
          with:
            virtual-pkg-name: grafana-oci-compat
            real-pkg-name: ${{subpkg.name}}
update:
  enabled: true
  version-transform:
    - match: ^(\d+\.\d+\.\d+)\+security-(\d+)
      replace: $1.$2
  github:
    identifier: grafana/grafana
    strip-prefix: v
    tag-filter-prefix: v13.0.
    use-tag: true
test:
  environment:
    contents:
      packages:
        - bash
        - curl
        - jq
        - procps
        - shadow
  pipeline:
    - uses: test/tw/ver-check
      with:
        bins: |
          grafana
        version: ${{vars.mangled-package-version}}
    - name: "Check Grafana version"
      runs: |
        grafana --help
        grafana cli --version | grep "${{vars.mangled-package-version}}"
        grafana cli --help
        grafana server --version | grep "${{vars.mangled-package-version}}"
        grafana server --help
    - name: "Test Grafana server startup"
      uses: test/daemon-check-output
      with:
        start: |
          grafana server \
            --config /etc/grafana/grafana.ini \
            --homepath /usr/share/grafana \
            --packaging=wolfi
        timeout: 60
        expected_output: |
          Starting Grafana
          HTTP Server Listen
        post: |
          curl -s http://localhost:3000/api/health
          curl -s http://localhost:3000/api/org
          curl -s http://localhost:3000/api/dashboards/home
    - name: "Check configuration file"
      runs: |
        grep -q "app_mode = production" /etc/grafana/grafana.ini
    - name: "Test Grafana CLI commands"
      runs: |
        grafana cli plugins list-remote
