# Generated from https://raw.githubusercontent.com/alpinelinux/aports/refs/heads/master/community/perl-mojolicious/APKBUILD
package:
  name: perl-mojolicious
  version: "9.41"
  epoch: 0
  description: Real-time web framework
  copyright:
    - license: Artistic-2.0
environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - perl
pipeline:
  - uses: fetch
    with:
      expected-sha256: ""
      uri: https://cpan.metacpan.org/authors/id/S/SR/SRI/Mojolicious-${{package.version}}.tar.gz
  - uses: perl/make
  - uses: autoconf/make
  - uses: autoconf/make-install
  - uses: perl/cleanup
  - uses: strip
subpackages:
  - name: perl-mojolicious-doc
    pipeline:
      - uses: split/manpages
    description: perl-mojolicious manpages
test:
  pipeline:
    - name: Verify perl-mojolicious installation, please improve the test as needed
      runs: perl -Mojo -E 'a(sub ($c) { $c->render(text => "Hello!") })->start' get / || exit 1
