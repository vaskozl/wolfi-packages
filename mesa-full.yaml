# yaml-language-server: $schema=https://raw.githubusercontent.com/chainguard-dev/melange/refs/heads/main/pkg/config/schema.json
package:
  name: mesa-full
  version: "26.1.1"
  epoch: 3 # split lib subpackages so mesa-gbm provider ships libgbm.so dev symlink
  description: Mesa DRI OpenGL/Vulkan, single unsplit package with Intel drivers (x86_64)
  copyright:
    - license: MIT AND SGI-B-2.0 AND BSL-1.0
  target-architecture:
    - x86_64
  dependencies:
    # Win over Wolfi's split mesa. The mesa-egl/gbm/gl/gles/glx/libgallium
    # virtuals are provided by the lib subpackages below (each at priority 10),
    # mirroring Wolfi's mesa.yaml file layout so we are a true drop-in.
    provider-priority: "10"
    provides:
      - mesa=${{package.full-version}}
    runtime:
      - libglvnd
      # CLC (Intel Vulkan) hard-requires the spirv-tools meson feature, which links
      # the unversioned libSPIRV-Tools.so soname. melange's SCA only emits versioned
      # so:NAME.so.N deps, so it can't auto-resolve this -- declare it explicitly.
      - spirv-tools
  resources:
    cpu: "14"
    memory: 13Gi
vars:
  llvm-ver: 19
  py-version: "3.13"
environment:
  contents:
    packages:
      - autoconf
      - automake
      - bison
      - build-base
      - busybox
      - ca-certificates-bundle
      - clang-${{vars.llvm-ver}}
      - elfutils-dev
      - expat-dev
      - findutils
      - flex
      - gettext
      - glslang-dev
      - libclang-cpp-${{vars.llvm-ver}}
      - libclc
      - libdrm-dev
      - libglvnd-dev
      - libtool
      - libva
      - libva-dev
      - libvdpau-dev
      - libx11-dev
      - libxcb-dev
      - libxdamage-dev
      - libxext-dev
      - libxfixes-dev
      - libxml2-dev
      - libxrandr
      - libxrandr-dev
      - libxrender
      - libxrender-dev
      - libxshmfence-dev
      - libxxf86vm-dev
      - llvm-${{vars.llvm-ver}}
      - llvm-${{vars.llvm-ver}}-dev
      - meson
      - py${{vars.py-version}}-mako
      - py${{vars.py-version}}-markupsafe
      - py${{vars.py-version}}-pygments
      - py${{vars.py-version}}-pyyaml
      - py${{vars.py-version}}-setuptools
      - python-${{vars.py-version}}
      - spirv-llvm-translator-dev
      - spirv-tools-dev
      - systemd-dev
      - vulkan-loader
      - wayland-dev
      # -dev ships wayland-protocols.pc; without it mesa downloads+installs a
      # bundled wayland-protocols subproject, leaking its files into our package.
      - wayland-protocols-dev
      - xorgproto
      - zlib-dev
      - zstd-dev
pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.freedesktop.org/mesa/mesa
      tag: mesa-${{package.version}}
  - runs: |
      export CFLAGS="$CFLAGS -O2 -g1"
      export CXXFLAGS="$CXXFLAGS -O2 -g1"
      export CPPFLAGS="$CPPFLAGS -O2 -g1"

      _dri_driverdir=/usr/lib/xorg/modules/dri
      _gallium_drivers="r300,r600,radeonsi,nouveau,llvmpipe,virgl,zink,svga,i915,iris,crocus"
      _vulkan_drivers="amd,swrast,intel,intel_hasvk,virtio"
      _vulkan_layers="device-select,overlay,intel-nullhw"

      meson \
        --prefix=/usr \
        -Ddri-drivers-path=$_dri_driverdir \
        -Dgallium-drivers=$_gallium_drivers \
        -Dvulkan-drivers=$_vulkan_drivers \
        -Dvulkan-layers=$_vulkan_layers \
        -Dplatforms=x11,wayland \
        -Dllvm=enabled \
        -Dglvnd=true \
        -Dshared-llvm=enabled \
        -Dshared-glapi=enabled \
        -Dgbm=enabled \
        -Dglx=dri \
        -Dopengl=true \
        -Dgles1=enabled \
        -Dgles2=enabled \
        -Degl=enabled \
        -Dgallium-extra-hud=true \
        -Dgallium-va=enabled \
        -Damdgpu-virtio=true \
        -Dallow-kcmp=enabled \
        -Dexpat=enabled \
        -Dxmlconfig=enabled \
        -Dxlib-lease=enabled \
        -Dshader-cache=enabled \
        -Dzstd=enabled \
        -Dvideo-codecs=all \
        -Dbackend_max_links=2 \
        -Db_ndebug=true \
        -Db_lto=false \
        . output

      meson configure --no-pager output
      meson compile -C output

      DESTDIR="${{targets.destdir}}" meson install --no-rebuild -C output
  - uses: strip
subpackages:
  # Runtime lib subpackages mirroring Wolfi's mesa.yaml layout. Each provides
  # the Wolfi virtual name at priority 10 so it wins over Wolfi's same-named
  # package despite our older version. Listed BEFORE -dev so their mv claims
  # the unversioned .so dev symlinks (notably libgbm.so) before split/dev runs.
  - name: ${{package.name}}-libgallium
    description: Mesa Gallium driver shared library (drop-in for Wolfi mesa-libgallium)
    dependencies:
      provider-priority: "10"
      provides:
        - mesa-libgallium=${{package.full-version}}
      runtime:
        - libglvnd
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}/usr/lib"
          mv "${{targets.destdir}}/usr/lib/libgallium-${{package.version}}.so"* "${{targets.subpkgdir}}/usr/lib/"
    test:
      pipeline:
        - uses: test/ldd-check
          with:
            packages: ${{package.name}}-libgallium
  - name: ${{package.name}}-egl
    description: Mesa EGL library (drop-in for Wolfi mesa-egl)
    dependencies:
      provider-priority: "10"
      provides:
        - mesa-egl=${{package.full-version}}
      runtime:
        - libglvnd
        - mesa-libgallium=${{package.full-version}}
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}/usr/lib"
          mv "${{targets.destdir}}/usr/lib/libEGL_mesa.so"* "${{targets.subpkgdir}}/usr/lib/"
    test:
      pipeline:
        - uses: test/ldd-check
          with:
            packages: ${{package.name}}-egl
  - name: ${{package.name}}-glx
    description: Mesa GLX library (drop-in for Wolfi mesa-glx)
    dependencies:
      provider-priority: "10"
      provides:
        - mesa-glx=${{package.full-version}}
      runtime:
        - libglvnd
        - mesa-libgallium=${{package.full-version}}
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}/usr/lib"
          mv "${{targets.destdir}}/usr/lib/libGLX_mesa.so"* "${{targets.subpkgdir}}/usr/lib/"
    test:
      pipeline:
        - uses: test/ldd-check
          with:
            packages: ${{package.name}}-glx
  # The libgbm.so* glob captures the unversioned libgbm.so dev symlink along
  # with libgbm.so.1* -- this is the fix for -lgbm on x86_64. The /usr/lib/gbm
  # DRI backend dir stays in the main package, matching Wolfi's mesa-gbm.
  - name: ${{package.name}}-gbm
    description: Mesa GBM library (drop-in for Wolfi mesa-gbm, incl. libgbm.so dev symlink)
    dependencies:
      provider-priority: "10"
      provides:
        - mesa-gbm=${{package.full-version}}
      runtime:
        - libglvnd
        - mesa-libgallium=${{package.full-version}}
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}/usr/lib"
          mv "${{targets.destdir}}/usr/lib/libgbm.so"* "${{targets.subpkgdir}}/usr/lib/"
    test:
      pipeline:
        - uses: test/ldd-check
          with:
            packages: ${{package.name}}-gbm
  # Transitive metapackages (no files), mirroring Wolfi's mesa-gl/mesa-gles.
  - name: ${{package.name}}-gl
    description: Mesa GL transitive package (drop-in for Wolfi mesa-gl)
    dependencies:
      provider-priority: "10"
      provides:
        - mesa-gl=${{package.full-version}}
      runtime:
        - ${{package.name}}=${{package.full-version}}
        - mesa-glx=${{package.full-version}}
    pipeline: []
  - name: ${{package.name}}-gles
    description: Mesa GLES transitive package (drop-in for Wolfi mesa-gles)
    dependencies:
      provider-priority: "10"
      provides:
        - mesa-gles=${{package.full-version}}
      runtime:
        - ${{package.name}}=${{package.full-version}}
        - mesa-glx=${{package.full-version}}
    pipeline: []
  - name: ${{package.name}}-dev
    description: ${{package.name}} development headers and libraries
    pipeline:
      - uses: split/dev
    dependencies:
      # Provide mesa-dev (so it can't be co-installed with Wolfi's mesa-dev and
      # the set stays coherent) but NOT at raised priority: mesa headers are
      # driver-agnostic, so trumping Wolfi's mesa-dev gives builds nothing while
      # dragging the fat runtime into build envs. Let Wolfi's lean mesa-dev win.
      provides:
        - mesa-dev=${{package.full-version}}
      runtime:
        - libglvnd-dev
        - ${{package.name}}=${{package.full-version}}
    test:
      pipeline:
        - uses: test/pkgconf
test:
  pipeline:
    - uses: test/ldd-check
      with:
        packages: ${{package.name}}
    - name: GPU drivers present
      runs: |
        set -eu
        # Software rasterizer Vulkan ICD.
        test -e /usr/share/vulkan/icd.d/lvp_icd.*.json
        # Intel Gallium (Iris Xe / 13th gen) + ANV/HASVK Vulkan ICDs.
        test -e /usr/lib/xorg/modules/dri/iris_dri.so
        test -e /usr/lib/libvulkan_intel.so
        test -e /usr/share/vulkan/icd.d/intel_icd.x86_64.json
        test -e /usr/lib/libvulkan_intel_hasvk.so
        test -e /usr/share/vulkan/icd.d/intel_hasvk_icd.x86_64.json
update:
  enabled: true
  git: {}
