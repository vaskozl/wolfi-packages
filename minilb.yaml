# yaml-language-server: $schema=https://raw.githubusercontent.com/chainguard-dev/melange/refs/heads/main/pkg/config/schema.json
package:
  name: minilb
  version: 0.2.2
  epoch: 1 # pin go-1.26 + go-package, trim build env
  description: Lightweight DNS based load balancer for Kubernetes
  copyright:
    - license: Apache-2.0
environment:
  contents:
    packages:
      - go-1.26
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/vaskozl/minilb
      tag: ${{package.version}}
  - uses: go/build
    with:
      go-package: go-1.26
      packages: cmd/main.go
      output: minilb
  - uses: strip
test:
  pipeline:
    - name: Verify minilb installation.
      runs: minilb -h || exit 1
