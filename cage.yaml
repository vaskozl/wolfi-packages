package:
  name: cage
  version: "0.3.0"
  epoch: 2 # functional test
  description: A Wayland kiosk
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - wlroots
environment:
  contents:
    packages:
      - build-base
      - meson
      - scdoc
      - wayland-protocols
      - wlroots
      - wlroots-dev
pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/cage-kiosk/cage
      tag: v${{package.version}}
  - runs: |
      ln -s /usr/bin/scdoc /usr/local/bin/scdoc
  - uses: meson/configure
  - uses: meson/compile
  - uses: meson/install
  - uses: strip
subpackages:
  - name: ${{package.name}}-doc
    pipeline:
      - uses: split/manpages
    description: cage manpages
test:
  pipeline:
    - runs: |
        set -eu
        cage -v
